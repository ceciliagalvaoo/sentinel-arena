NEXT_PUBLIC_API_URL=http://localhost:4000

# Single source of truth for every Solscan link in the app — must match
# whichever network apps/backend-api and the agents are actually pointed at.
# Never hardcode "devnet" in a component; this is the only place it's set.
NEXT_PUBLIC_SOLANA_CLUSTER=devnet
