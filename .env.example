# Root-level env — used by scripts/ (migrate.ts, setup-subscription.ts) and
# as the source of truth copied into apps/agent-*/.env and docker-compose.
# Per-app envs (apps/agent-aggressive/.env.example, apps/agent-conservative/.env.example)
# override AGENT_ID/SENSITIVITY_MULTIPLIER/WINDOW_SECONDS/WALLET_KEYPAIR_PATH.

NETWORK=devnet
SOLANA_RPC_URL=https://api.devnet.solana.com
TXLINE_API_ORIGIN=https://txline-dev.txodds.com
TXLINE_PROGRAM_ID=6pW64gN1s2uqjHkn1unFeEjAwJkPGHoppGvS715wyP2J
TXLINE_TOKEN_MINT=4Zao8ocPhmMgq7PdsYWyxvqySMGx7xb9cMftPMkEokRG

DATABASE_URL=postgres://postgres:devpassword@localhost:5432/sentinel

# backend-api
PORT=4000

# dashboard
NEXT_PUBLIC_API_URL=http://localhost:4000

# auto-filled by scripts/setup-subscription.ts on first run -- never commit
# real values here
TXLINE_GUEST_JWT=
TXLINE_API_TOKEN=
